import React, {FC} from 'react';
import './Header.css';

const Header: FC = (() => {
    return (
        <header>
            <div className='header-title'>Shopping Cart</div>
        </header>
    );
});

export default Header;
